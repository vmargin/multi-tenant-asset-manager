# Railway deployment configuration
# IMPORTANT: Set Root Directory to "backend" in Railway dashboard Settings

[build]
builder = "nixpacks"

[deploy]
startCommand = "npm start"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
